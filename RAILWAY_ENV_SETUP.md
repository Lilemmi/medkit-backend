# 🔧 Настройка переменных окружения на Railway

## ⚠️ Ошибка 500 - скорее всего проблема с переменными окружения!

## ✅ Что нужно настроить в Railway:

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на https://railway.app
2. Откройте проект
3. Нажмите на **сервис с бэкендом** (NestJS)

### Шаг 2: Настройте переменные окружения

Перейдите в **Settings** → **Variables** и добавьте:

#### 1. DATABASE_URL (обязательно!)

**Вариант A: Использовать internal URL (рекомендуется):**
```
postgresql://postgres:atSNeqIROELQAtAAXFYiUmRBiyxtoWya@postgres.railway.internal:5432/railway
```

**Вариант B: Использовать public URL:**
```
postgresql://postgres:atSNeqIROELQAtAAXFYiUmRBiyxtoWya@shuttle.proxy.rlwy.net:34466/railway
```

**Как получить:**
- В Railway dashboard → PostgreSQL сервис
- Settings → Variables
- Найдите `DATABASE_URL` или `POSTGRES_URL`
- Скопируйте значение

#### 2. JWT_SECRET (обязательно!)

```
your-super-secret-jwt-key-change-this-in-production
```

Или любой другой секретный ключ (минимум 32 символа).

#### 3. PORT (опционально)

Railway обычно устанавливает автоматически, но можно добавить:
```
3000
```

### Шаг 3: Выполните миграции Prisma

После настройки `DATABASE_URL`, нужно выполнить миграции:

**Вариант A: Добавить в Build Command**

1. Settings → Deploy
2. Build Command:
   ```
   npx prisma generate && npx prisma migrate deploy && npm run build
   ```

**Вариант B: Добавить в package.json**

Добавьте в `package.json`:
```json
"scripts": {
  "postinstall": "npx prisma generate",
  "migrate": "npx prisma migrate deploy"
}
```

### Шаг 4: Перезапустите сервис

После изменения переменных окружения:
- Railway автоматически перезапустит сервис
- Или нажмите "Redeploy" вручную

## ✅ Проверка:

### 1. Проверьте логи Railway

1. Зайдите на https://railway.app
2. Откройте проект → сервис с бэкендом
3. Перейдите в **"Logs"**
4. Должно быть:
   ```
   🚀 Server is running on port 3000
   ```
   Без ошибок подключения к базе данных!

### 2. Проверьте бэкенд в браузере

```
https://medkit-backend-production.up.railway.app
```

Должно быть:
```json
{"status":"API works 🎉"}
```

### 3. Проверьте login endpoint

Попробуйте войти в приложение - ошибка 500 должна исчезнуть!

## 🔍 Если все еще ошибка 500:

### Проверьте логи Railway

В логах должна быть детальная информация:
- Если "Can't reach database server" → проблема с `DATABASE_URL`
- Если "relation does not exist" → нужно выполнить миграции
- Если другая ошибка → пришлите логи

## 📝 Итого:

**Обязательные переменные окружения:**
- ✅ `DATABASE_URL` - из PostgreSQL сервиса
- ✅ `JWT_SECRET` - ваш секретный ключ

**После настройки:**
- ✅ Выполните миграции Prisma
- ✅ Перезапустите сервис
- ✅ Проверьте логи

**После этого ошибка 500 должна исчезнуть!** 🚀

