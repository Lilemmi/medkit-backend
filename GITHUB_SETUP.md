# 🔧 Настройка для работы с GitHub

## 📝 Что нужно для работы с репозиторием:

### 1. **Git репозиторий должен быть инициализирован**

Проверьте:
```bash
cd medkit-backend
git status
```

Если репозиторий не инициализирован:
```bash
git init
```

### 2. **Связь с GitHub репозиторием**

Если репозиторий уже на GitHub:
```bash
# Проверьте remote
git remote -v

# Если нет remote, добавьте:
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
```

Если создаете новый репозиторий:
```bash
# 1. Создайте репозиторий на GitHub
# 2. Добавьте remote:
git remote add origin https://github.com/ВАШ_USERNAME/medkit-backend.git
```

### 3. **Файлы для коммита**

Убедитесь, что `.env` в `.gitignore`:
```bash
# Проверьте .gitignore
cat .gitignore | grep .env
```

Если нет, добавьте в `.gitignore`:
```
.env
.env.local
*.log
node_modules/
dist/
```

### 4. **Структура для работы**

Я могу работать с файлами, если:
- ✅ Файлы находятся в рабочей директории проекта
- ✅ У меня есть доступ к файлам через инструменты чтения/записи
- ✅ Git репозиторий настроен (для отслеживания изменений)

## 🚀 Что я могу делать:

### ✅ Могу:
- Читать и редактировать файлы в `medkit-backend/`
- Создавать новые файлы
- Обновлять код
- Запускать команды в терминале
- Проверять и исправлять ошибки

### ❌ Не могу напрямую:
- Делать git push/pull (но могу подготовить команды)
- Управлять GitHub репозиторием напрямую
- Получать доступ к приватным репозиториям без токенов

## 📝 Для деплоя на Railway через GitHub:

1. **Убедитесь, что код в GitHub:**
   ```bash
   git add .
   git commit -m "Ready for Railway deployment"
   git push origin main
   ```

2. **В Railway:**
   - New Project → Deploy from GitHub repo
   - Выберите репозиторий
   - Railway автоматически задеплоит

3. **Настройте переменные окружения в Railway:**
   - `DATABASE_URL` - уже есть из PostgreSQL
   - `JWT_SECRET` - ваш секретный ключ

## 🔍 Текущая проблема:

Приложение пытается подключиться к локальному серверу, но он не запущен.

**Решение:**
1. Запустите бэкенд локально:
   ```bash
   cd medkit-backend
   npm run start:dev
   ```

2. Или настройте Railway URL в `app.json` для использования облачного API

## 💡 Рекомендация:

Для разработки используйте локальный сервер, для продакшена - Railway.

