# Деплой бэкенда на Railway

## 🚀 Быстрый деплой

### Вариант 1: Деплой через GitHub (рекомендуется)

1. **Подготовьте репозиторий:**
   ```bash
   # Убедитесь, что все изменения закоммичены
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **В Railway:**
   - Зайдите на https://railway.app
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Railway автоматически определит NestJS и задеплоит

3. **Настройте переменные окружения:**
   - В настройках проекта добавьте:
     - `DATABASE_URL` - уже есть (из PostgreSQL сервиса)
     - `JWT_SECRET` - ваш секретный ключ
     - `PORT` - Railway установит автоматически

4. **Подключите PostgreSQL:**
   - В настройках проекта нажмите "Add Service" → "PostgreSQL"
   - Railway автоматически создаст переменную `DATABASE_URL`
   - Или используйте существующую PostgreSQL базу

### Вариант 2: Деплой через Railway CLI

1. **Установите Railway CLI:**
   ```bash
   npm i -g @railway/cli
   ```

2. **Войдите:**
   ```bash
   railway login
   ```

3. **Инициализируйте проект:**
   ```bash
   cd medkit-backend
   railway init
   ```

4. **Добавьте переменные окружения:**
   ```bash
   railway variables set DATABASE_URL="postgresql://..."
   railway variables set JWT_SECRET="your-secret-key"
   ```

5. **Задеплойте:**
   ```bash
   railway up
   ```

## 📝 Настройка для продакшена

### 1. Создайте `railway.json` (опционально):

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### 2. Обновите `package.json` для продакшена:

Убедитесь, что есть скрипт:
```json
{
  "scripts": {
    "start:prod": "node dist/main"
  }
}
```

### 3. Переменные окружения в Railway:

- `DATABASE_URL` - автоматически из PostgreSQL сервиса
- `JWT_SECRET` - ваш секретный ключ (сгенерируйте надежный)
- `PORT` - Railway установит автоматически (обычно через переменную `PORT`)

### 4. После деплоя:

1. Railway даст вам URL типа: `https://your-app.railway.app`
2. Обновите `EXPO_PUBLIC_API_URL` в мобильном приложении на этот URL
3. Или используйте переменную окружения в `.env` файле мобильного приложения

## 🔧 Локальная разработка

Для локальной разработки все еще нужно запускать:

```bash
cd medkit-backend
npm run start:dev
```

И обновить `.env` в мобильном приложении:
```env
EXPO_PUBLIC_API_URL=http://localhost:3000
# или для физического устройства:
EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:3000
```

## ✅ Проверка работы

После деплоя проверьте:

1. **Health check:**
   ```bash
   curl https://your-app.railway.app
   ```

2. **API endpoints:**
   - `GET /medicines/:userId` - список лекарств
   - `POST /auth/register` - регистрация
   - `POST /auth/login` - вход

## 📱 Обновление мобильного приложения

После деплоя обновите URL API в мобильном приложении:

1. Создайте `.env` в корне проекта:
   ```env
   EXPO_PUBLIC_API_URL=https://your-app.railway.app
   ```

2. Или обновите `src/api/api.ts`:
   ```typescript
   const baseURL = "https://your-app.railway.app";
   ```

## 🎯 Итого

- **Для разработки:** запускайте локально `npm run start:dev`
- **Для продакшена:** задеплойте на Railway
- **PostgreSQL:** уже работает на Railway
- **Мобильное приложение:** обновите URL API на Railway URL

После деплоя бэкенд будет работать 24/7, и вам не нужно будет запускать его локально! 🚀

