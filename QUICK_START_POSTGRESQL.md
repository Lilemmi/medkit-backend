# Быстрый старт с PostgreSQL

## ✅ Что было сделано:

1. ✅ Миграция схемы Prisma с SQLite на PostgreSQL
2. ✅ Добавлена модель `InventoryHistory` для истории инвентаризации
3. ✅ Обновлен сервис лекарств с автоматическим логированием истории
4. ✅ Добавлены новые API эндпоинты для истории
5. ✅ Добавлены индексы для производительности
6. ✅ Установлены зависимости (pg, @types/pg)

## 🚀 Быстрый старт:

### 1. Установите PostgreSQL

**Windows:**
- Скачайте с https://www.postgresql.org/download/windows/
- Или: `choco install postgresql`

**macOS:**
```bash
brew install postgresql
brew services start postgresql
```

**Linux:**
```bash
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

### 2. Создайте базу данных

```bash
psql -U postgres
CREATE DATABASE medkit_db;
\q
```

### 3. Настройте .env

Создайте файл `medkit-backend/.env`:

```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/medkit_db"
JWT_SECRET="your-secret-key"
PORT=3000
```

### 4. Примените миграции

```bash
cd medkit-backend
npx prisma generate
npx prisma migrate dev --name init_postgresql
```

### 5. Запустите приложение

```bash
npm run start:dev
```

## 📊 Новые возможности:

### История инвентаризации

Все действия автоматически записываются:
- ✅ Создание лекарства
- ✅ Обновление лекарства  
- ✅ Удаление лекарства

### Новые API эндпоинты:

```bash
# Вся история пользователя
GET /medicines/:userId/history

# История конкретного лекарства
GET /medicines/:userId/history/:medicineId
```

## 🔒 Разделение данных по пользователям

Все запросы автоматически фильтруются по `userId`:
- Каждый пользователь видит только свои данные
- История привязана к пользователю
- Полная изоляция данных

## 📈 Производительность

Добавлены индексы для быстрого поиска:
- По email пользователя
- По userId для лекарств
- По дате истечения срока
- По действиям в истории

## 🌐 Деплой в продакшен

См. подробное руководство: `MIGRATION_GUIDE.md`

**Рекомендуемые платформы:**
- Railway (https://railway.app) - самый простой
- Render (https://render.com) - хороший бесплатный вариант
- Supabase (https://supabase.com) - PostgreSQL + дополнительные функции

## 📝 Документация

- `MIGRATION_GUIDE.md` - подробное руководство по миграции
- `ENV_SETUP.md` - настройка переменных окружения

