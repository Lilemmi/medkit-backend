# 🔍 Диагностика ошибки 500

## ✅ Хорошие новости:

- ✅ Приложение подключается к Railway
- ✅ URL правильный: `https://medkit-backend-production.up.railway.app`
- ✅ Запрос доходит до сервера

## 🔴 Проблема:

Бэкенд возвращает ошибку 500 "Internal server error" при попытке входа.

## 🔍 Возможные причины:

### 1. База данных не подключена

**Проверьте:**
- В Railway dashboard → сервис с бэкендом
- Settings → Variables
- Есть ли `DATABASE_URL`?

**Решение:**
- Добавьте `DATABASE_URL` в переменные окружения Railway
- Используйте internal URL: `postgresql://postgres:password@postgres.railway.internal:5432/railway`

### 2. Prisma миграции не выполнены

**Проверьте:**
- В Railway dashboard → сервис с бэкендом
- Logs → есть ли ошибки про Prisma?

**Решение:**
- Выполните миграции на Railway
- Или добавьте команду миграции в build процесс

### 3. JWT_SECRET не настроен

**Проверьте:**
- В Railway dashboard → сервис с бэкендом
- Settings → Variables
- Есть ли `JWT_SECRET`?

**Решение:**
- Добавьте `JWT_SECRET` в переменные окружения Railway

### 4. Ошибка в коде бэкенда

**Проверьте:**
- В Railway dashboard → сервис с бэкендом
- Logs → посмотрите детали ошибки

## ✅ Решение:

### Шаг 1: Проверьте логи Railway

1. Зайдите на https://railway.app
2. Откройте проект → сервис с бэкендом
3. Перейдите в **"Logs"**
4. Посмотрите последние ошибки
5. Найдите детали ошибки 500

### Шаг 2: Проверьте переменные окружения

В Railway dashboard → сервис с бэкендом → Settings → Variables:

**Должны быть:**
- `DATABASE_URL` - из PostgreSQL сервиса
- `JWT_SECRET` - ваш секретный ключ
- `PORT` - обычно Railway устанавливает автоматически

### Шаг 3: Проверьте миграции

Если база данных пустая, нужно выполнить миграции:

```bash
# В Railway можно добавить команду в build:
npx prisma migrate deploy
```

Или выполнить вручную через Railway CLI.

## 🎯 Быстрая проверка:

### Проверьте бэкенд в браузере:

```
https://medkit-backend-production.up.railway.app
```

Должно быть:
```json
{"status":"API works 🎉"}
```

### Проверьте логи Railway:

Там должна быть детальная информация об ошибке 500.

## 📝 Следующие шаги:

1. **Проверьте логи Railway** - там будет детальная информация
2. **Проверьте переменные окружения** - все ли настроено
3. **Проверьте миграции** - выполнены ли они

**Пришлите логи из Railway - я помогу исправить проблему!**

