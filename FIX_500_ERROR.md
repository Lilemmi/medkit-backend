# 🔧 Исправление ошибки 500

## ✅ Хорошие новости:

- ✅ Приложение подключается к Railway
- ✅ URL правильный
- ✅ Запрос доходит до сервера

## 🔴 Проблема:

Бэкенд возвращает 500 "Internal server error" при попытке входа.

## 🔍 Наиболее вероятные причины:

### 1. База данных не подключена (90% вероятность)

**Симптомы:**
- Prisma не может подключиться к базе данных
- Ошибка в логах: "Can't reach database server"

**Решение:**

1. **Проверьте переменные окружения в Railway:**
   - Зайдите на https://railway.app
   - Откройте проект → сервис с бэкендом
   - Settings → Variables
   - Должна быть переменная `DATABASE_URL`

2. **Если нет `DATABASE_URL`:**
   - Добавьте переменную `DATABASE_URL`
   - Используйте internal URL от PostgreSQL сервиса:
     ```
     postgresql://postgres:password@postgres.railway.internal:5432/railway
     ```
   - Или используйте public URL:
     ```
     postgresql://postgres:password@shuttle.proxy.rlwy.net:34466/railway
     ```

### 2. Prisma миграции не выполнены (80% вероятность)

**Симптомы:**
- Таблицы не существуют в базе данных
- Ошибка: "relation does not exist"

**Решение:**

1. **В Railway dashboard:**
   - Откройте сервис с бэкендом
   - Settings → Deploy
   - Добавьте команду в "Build Command":
     ```
     npx prisma generate && npx prisma migrate deploy
     ```

2. **Или выполните вручную:**
   - В Railway dashboard → сервис с бэкендом
   - Deployments → View Logs
   - Или используйте Railway CLI

### 3. JWT_SECRET не настроен (50% вероятность)

**Решение:**

1. **В Railway dashboard:**
   - Settings → Variables
   - Добавьте `JWT_SECRET` с любым секретным ключом
   - Например: `your-super-secret-jwt-key-change-this-in-production`

### 4. Ошибка в коде

**Решение:**

1. **Проверьте логи Railway:**
   - Зайдите на https://railway.app
   - Откройте проект → сервис с бэкендом
   - Перейдите в **"Logs"**
   - Посмотрите детали ошибки 500

## ✅ Пошаговое решение:

### Шаг 1: Проверьте переменные окружения

В Railway dashboard → сервис с бэкендом → Settings → Variables:

**Должны быть:**
- ✅ `DATABASE_URL` - из PostgreSQL сервиса
- ✅ `JWT_SECRET` - ваш секретный ключ
- ✅ `PORT` - обычно Railway устанавливает автоматически

### Шаг 2: Проверьте логи Railway

1. Зайдите на https://railway.app
2. Откройте проект → сервис с бэкендом
3. Перейдите в **"Logs"**
4. Посмотрите последние ошибки
5. Найдите детали ошибки 500

### Шаг 3: Выполните миграции

Если база данных пустая:

1. **В Railway dashboard:**
   - Settings → Deploy
   - Добавьте в "Build Command":
     ```
     npx prisma generate && npx prisma migrate deploy && npm run build
     ```

2. **Или добавьте в package.json:**
   ```json
   "scripts": {
     "postinstall": "npx prisma generate",
     "migrate": "npx prisma migrate deploy"
   }
   ```

### Шаг 4: Перезапустите сервис

После изменения переменных окружения или build команды:
- Railway автоматически перезапустит сервис
- Или нажмите "Redeploy" вручную

## 🎯 Быстрая проверка:

### Проверьте бэкенд в браузере:

```
https://medkit-backend-production.up.railway.app
```

Должно быть:
```json
{"status":"API works 🎉"}
```

### Проверьте подключение к базе:

Если бэкенд работает, но login падает - скорее всего проблема с базой данных или миграциями.

## 📝 Что делать сейчас:

1. **Проверьте логи Railway** - там будет детальная информация об ошибке
2. **Проверьте переменные окружения** - все ли настроено
3. **Проверьте миграции** - выполнены ли они

**Пришлите логи из Railway - я помогу исправить проблему!**

